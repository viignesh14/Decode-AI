{
  "name": "decode-ai-backend",
  "version": "1.0.0",
  "description": "Decode AI backend proxy — keeps Gemini API key secure on the server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
